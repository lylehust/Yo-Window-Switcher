# Yo Window Switcher

![App Screenshot](YoWindowSwitcher.png)

一款 macOS 键盘驱动的窗口切换器。按下快捷键即可弹出悬浮磨砂玻璃面板，显示所有打开的窗口并配有 Vimium 风格提示键——按下对应提示键即可瞬间切换到目标窗口。

## 工作原理

1. 按下 **Option+w**（可自定义）打开切换面板
2. 每个打开的窗口都会获得一个提示键标签（A, S, D, F, ...）
3. 按下对应的提示键即可切换到该窗口
4. 按下 **Escape** 或者快捷键关闭面板

## 功能特性

- **窗口缩略图** — 真实窗口预览
- **按窗口切换** — 每个屏幕上的窗口都有独立入口，而非仅按应用分组
- **自动适配网格** — 4 列网格布局，缩略图大小自动调整以填满面板，无需滚动
- **悬浮磨砂玻璃面板** — 居中显示，宽度占屏幕 80%
- **Vimium 风格提示键** — 优先使用主行键位，触手可及
- **可自定义快捷键** — 在偏好设置中录制任意组合键
- **开机启动** — 在菜单栏中一键开关
- **冲突检测** — 检测快捷键是否与系统快捷键冲突并给出警告
- **仅菜单栏图标** — 无 Dock 图标，常驻运行，轻量无感
- **通用二进制** — 同时支持 Apple Silicon 和 Intel Mac

## 所需权限

- **屏幕录制** — 用于获取窗口实时预览
- **辅助功能** — 用于按窗口切换（将指定窗口置于最前）

## 系统要求

- macOS 14 (Sonoma) 或更高版本

## 安装

1. 打开 DMG 文件
2. 将 **Yo Window Switcher** 拖入 **应用程序** 文件夹
3. 从应用程序文件夹启动
4. 根据提示授予屏幕录制和辅助功能权限

## 使用方法

| 操作 | 快捷键 |
|------|--------|
| 打开窗口切换器 | Option+w |
| 切换到目标窗口 | 按下对应提示键 |
| 关闭面板 | Escape / 快捷键 |
| 偏好设置 | 菜单栏 → Preferences... |

## 版本历史

**0.2** — 按窗口切换、4 列自动适配网格  
**0.1** — 初始发布
